# HomeDash runs on a dedicated Raspberry Pi with limited flash storage.
# Native dumps are disabled after the application crash has been corrected;
# systemd still records the terminating signal and restart result in the journal.
kernel.core_pattern=/dev/null
kernel.core_uses_pid=0
fs.suid_dumpable=0
